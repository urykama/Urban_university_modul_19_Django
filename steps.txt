python -m venv .venv
    # для создания вирт. среды в Pycharm выбери ПИТОНа в interpreter settings

.\\.venv\Scripts\activate
deactivate
python -m pip install --upgrade pip
pip install Django~=4.1.0

    Она создаст проект Django с именем UrbanDjango.
django-admin startproject UrbanDjango

cd  UrbanDjango

    Создание приложения
python manage.py startapp task1

python manage.py runserver

python manage.py makemigrations task1
python manage.py migrate


python manage.py shell
from task1.models import Buyer
Buyer.objects.all()
Buyer.objects.create(name='first', balance = 3000, age = 20)
Buyer.objects.create(name='Terminator2000', balance = 42.15, age = 52)
Buyer.objects.create(name='Ubivator432', balance = 0.5, age = 16)

>>> a = Buyer.objects.filter(name='first')
>>> a
>>> a.update(name='Ilya')
>>> Buyer.objects.filter(name='Ilya').update(balance = 1500.05, age = 24)


from task1.models import Game
Game.objects.all()
Game.objects.create(title='Cyberpunk2077', cost = 31, size = 46.2, description='Game of the year', age_limited=1)
Game.objects.create(title='Mario', cost = 5, size = 0.5, description='Old game', age_limited=0)
Game.objects.create(title='Hitman', cost = 12, size = 36.6, description='Who kills Mark?', age_limited=1)

Game.objects.get(id=3).buyer.set((4, 3))
Game.objects.get(id=2).buyer.set((2, 3))
Game.objects.get(id=4).buyer.set((2, 3))

exit()